name: mit_flutter_plugin
description: "MIT Map services plugin for flutter."
version: 1.0.0
homepage: https://metaindonesia.technology

environment:
  sdk: ^3.5.0
  flutter: '>=3.3.0'

dependencies:
  ffi: ^2.1.2
  flutter:
    sdk: flutter
  geolocator: ^13.0.1
  package_info_plus: ^4.2.0
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  ffigen: ^11.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  plugin:
    platforms:
      android:
        ffiPlugin: true
      ios:
        ffiPlugin: true
        podspec: ios/mit_flutter_plugin.podspec
        

